/*
 * 页头
 * 功能：
 *   1. 判断全局登陆态
 *   2. 显示头像 用户名
 *   3. 退出登录
 *
 */
import React from 'react';
import './layout.scss';

const Header = props => {
  console.log(props);
  return (
    <div className="header">
      Demo - Header
    </div>
  );
};

export default Header;
